/**
 * OBSOLETO — Usa el script principal de actividades:
 *   scripts/activities-management.gs
 *
 * Ese archivo ya incluye doGet() al final para la web.
 * El calendario configurado es CONFIG.CALENDAR_ID = "somoslavoztc@gmail.com"
 *
 * Pasos:
 * 1. Abre tu proyecto Apps Script vinculado a la hoja de actividades
 * 2. Asegúrate de tener la función doGet() al final (copia desde activities-management.gs)
 * 3. Desplegar → Nueva implementación → Aplicación web
 *      Ejecutar como: Yo
 *      Quién tiene acceso: Cualquier persona
 * 4. GOOGLE_CALENDAR_WEBAPP_URL=https://script.google.com/macros/s/.../exec
